<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">
    <changeSet author="straumat" id="0.1.0-table-requests-add-request">

        <createTable tableName="REQUESTS_ADD_ASSET"
                     remarks="A request to add an asset to royllo explorer">

            <!-- Unique identifier -->
            <column name="ID" type="BIGINT"
                    remarks="Unique identifier">
                <constraints nullable="false" primaryKey="true" primaryKeyName="PK_REQUESTS_ADD_ASSET"/>
            </column>

            <!-- Table fields -->
            <column name="GENESIS_POINT" type="VARCHAR(255)"
                    remarks="The first outpoint of the transaction that created the asset (txid:vout)"/>
            <column name="NAME" type="VARCHAR(255)"
                    remarks="The name of the asset"/>
            <column name="META_DATA" type="VARCHAR(255)"
                    remarks="The hashed metadata of the asset"/>
            <column name="ASSET_ID" type="VARCHAR(255)"
                    remarks="The asset ID that uniquely identifies the asset"/>
            <column name="OUTPUT_INDEX" type="INTEGER"
                    remarks="The index of the output that carries the unique Taro commitment in the genesis transaction"/>
            <column name="PROOF" type="VARCHAR(255)"
                    remarks="Proof that validates the asset information"/>

            <!-- Technical fields -->
            <column name="CREATED_ON" type="TIMESTAMP WITH TIME ZONE"
                    remarks="Data created on"/>
            <column name="UPDATED_ON" type="TIMESTAMP WITH TIME ZONE"
                    remarks="Data updated on"/>

        </createTable>

        <!-- Foreign key REQUESTS_ADD_ASSET.ID > REQUESTS.ID -->
        <addForeignKeyConstraint constraintName="FK_REQUESTS_ADD_ASSET_REQUESTS_ID"
                                 baseColumnNames="ID"
                                 baseTableName="REQUESTS_ADD_ASSET"
                                 deferrable="false"
                                 initiallyDeferred="false"
                                 onDelete="NO ACTION"
                                 onUpdate="NO ACTION"
                                 referencedTableName="REQUESTS"
                                 referencedColumnNames="ID"
                                 validate="true"/>

    </changeSet>
</databaseChangeLog>